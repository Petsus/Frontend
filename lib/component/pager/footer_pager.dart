
import 'package:flutter/material.dart';

class FooterPager extends StatelessWidget {
  const FooterPager({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Row(
      children: [

      ],
    );
  }
}
